<?php

// immutable
$students = ['Robiul', "Mizan", "Limon"];

$students[0] = "Mizan";

var_dump($students);


// Delete and add a value on array

// array_shift(); -> remove the first element
// array_pop(); -> remove the last element
// array_push(); -> add a new element at the end of the array
// array_unshift(); -> add a new element at the beginning of the array